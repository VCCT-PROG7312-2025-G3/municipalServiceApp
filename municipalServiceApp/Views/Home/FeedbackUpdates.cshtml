<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback / Updates</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #7b4dff;
            --primary-light: #9370ff;
            --secondary: #18a0fb;
            --success: #00c9a7;
            --warning: #ffc107;
            --info: #17a2b8;
            --light-bg: #f8f9fa;
            --card-bg: #ffffff;
            --text-primary: #333333;
            --text-secondary: #6c757d;
            --border-color: #e9ecef;
        }

        body {
            background: linear-gradient(135deg, #f9f9f9 0%, #f0f4ff 100%);
            color: var(--text-primary);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            padding: 20px 0;
            border-bottom: 1px solid var(--primary-light);
        }

        .filter-card {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 25px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--border-color);
        }

            .filter-card label {
                color: var(--text-primary);
                margin-bottom: 8px;
                font-weight: 500;
            }

        .form-control {
            background-color: var(--light-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            border-radius: 8px;
            padding: 10px 15px;
        }

            .form-control:focus {
                background-color: var(--card-bg);
                border-color: var(--primary);
                color: var(--text-primary);
                box-shadow: 0 0 0 0.2rem rgba(123, 77, 255, 0.15);
            }

        .btn {
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 600;
            border: none;
            transition: all 0.3s;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary) 0%, #6240cf 100%);
            color: white;
        }

        .btn-secondary {
            background: linear-gradient(135deg, #94a1b2 0%, #727f8f 100%);
            color: white;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .table-container {
            background: var(--card-bg);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--border-color);
        }

        .table {
            margin-bottom: 0;
        }

            .table th {
                background: linear-gradient(135deg, var(--primary) 0%, #6240cf 100%);
                color: white;
                border: none;
                padding: 15px;
                font-weight: 600;
            }

            .table td {
                padding: 15px;
                border-color: var(--border-color);
                vertical-align: middle;
                color: var(--text-primary);
            }

        .table-hover tbody tr:hover {
            background-color: rgba(123, 77, 255, 0.05);
        }

        .badge {
            padding: 8px 12px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.85rem;
        }

        h1 {
            color: var(--text-primary);
            font-weight: 700;
        }

        .lead {
            color: var(--text-secondary);
        }

        .status-badge {
            display: inline-block;
            min-width: 110px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1><i class="fas fa-comments me-2" style="color: var(--primary);"></i>Feedback / Updates</h1>
            <p class="lead">Track the status of reported issues in your community</p>
        </div>

        <!-- Filters -->
        <div class="filter-card">
            <form method="get" asp-action="FeedbackUpdates" class="row g-3">
                <div class="col-md-4">
                    <label>Filter by Area</label>
                    <input type="text" name="searchLocation" class="form-control" placeholder="Enter location...">
                </div>
                <div class="col-md-4">
                    <label>Filter by Category</label>
                    <input type="text" name="searchCategory" class="form-control" placeholder="Enter category...">
                </div>
                <div class="col-md-4">
                    <label>Filter by Date</label>
                    <input type="date" name="searchDate" class="form-control">
                </div>
                <div class="col-12 text-center mt-4">
                    <button type="submit" class="btn btn-primary me-2"><i class="fas fa-filter me-2"></i>Filter</button>
                    <a asp-action="FeedbackUpdates" class="btn btn-secondary"><i class="fas fa-sync me-2"></i>Reset</a>
                </div>
            </form>
        </div>

        <!-- Issues Table -->
        <div class="table-container">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Date Reported</th>
                            <th>Location</th>
                            <th>Category</th>
                            <th>Description</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var issue in Model)
                        {
                            <tr>
                                <td>@issue.DateReported.ToString("yyyy-MM-dd")</td>
                                <td>@issue.Location</td>
                                <td>@issue.Category</td>
                                <td>@issue.Description</td>
                                <td>
                                    <span class="badge bg-primary status-badge">Pending</span>
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>