<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Municipal Service App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #7b4dff;
            --primary-dark: #6240cf;
            --info: #18a0fb;
            --info-dark: #0f7cb3;
            --success: #00c9a7;
            --success-dark: #008f78;
            --text-dark: #4a4a4a;
            --text-medium: #6b6b6b;
            --text-light: #555;
            --shadow-light: rgba(0,0,0,0.08);
            --shadow-medium: rgba(0,0,0,0.12);
            --shadow-dark: rgba(0,0,0,0.15);
        }

        body {
            background: linear-gradient(145deg, #f3f4ff 0%, #ffffff 100%);
            color: #333;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            padding: 20px;
            line-height: 1.6;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 60px;
            padding: 30px 0;
            position: relative;
        }

            .header::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 50%;
                transform: translateX(-50%);
                width: 100px;
                height: 4px;
                background: linear-gradient(to right, var(--primary), var(--info), var(--success));
                border-radius: 2px;
            }

            .header h1 {
                font-size: 2.8rem;
                font-weight: 800;
                color: var(--text-dark);
                margin-bottom: 1rem;
                letter-spacing: -0.5px;
            }

            .header p {
                font-size: 1.2rem;
                color: var(--text-medium);
                max-width: 600px;
                margin: 0 auto;
            }

        .app-card {
            background: #fff;
            border-radius: 18px;
            padding: 35px 30px;
            margin-bottom: 35px;
            text-align: center;
            box-shadow: 0 10px 30px var(--shadow-light);
            border: none;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
        }

            .app-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 5px;
                background: linear-gradient(to right, var(--primary), var(--info), var(--success));
                opacity: 0;
                transition: opacity 0.3s;
            }

            .app-card:hover::before {
                opacity: 1;
            }

            .app-card:hover {
                transform: translateY(-8px);
                box-shadow: 0 15px 35px var(--shadow-medium);
            }

            .app-card h3 {
                font-weight: 700;
                margin-bottom: 15px;
                font-size: 1.5rem;
                color: var(--text-dark);
            }

            .app-card p {
                color: var(--text-light);
                margin-bottom: 25px;
                font-size: 1.05rem;
            }

        .card-icon {
            font-size: 2.5rem;
            margin-bottom: 20px;
            display: inline-block;
            width: 80px;
            height: 80px;
            line-height: 80px;
            border-radius: 50%;
            background: rgba(123, 77, 255, 0.1);
            color: var(--primary);
        }

            .card-icon.info {
                background: rgba(24, 160, 251, 0.1);
                color: var(--info);
            }

            .card-icon.success {
                background: rgba(0, 201, 167, 0.1);
                color: var(--success);
            }

        .btn {
            padding: 14px 32px;
            border-radius: 50px;
            font-weight: 600;
            margin: 8px 5px;
            border: none;
            transition: all 0.3s;
            font-size: 1rem;
            letter-spacing: 0.5px;
            position: relative;
            overflow: hidden;
        }

            .btn::after {
                content: '';
                position: absolute;
                top: 50%;
                left: 50%;
                width: 5px;
                height: 5px;
                background: rgba(255, 255, 255, 0.5);
                opacity: 0;
                border-radius: 100%;
                transform: scale(1, 1) translate(-50%);
                transform-origin: 50% 50%;
            }

            .btn:focus:not(:active)::after {
                animation: ripple 1s ease-out;
            }

        @@keyframes ripple {
            0%

        {
            transform: scale(0, 0);
            opacity: 0.5;
        }

        100% {
            transform: scale(20, 20);
            opacity: 0;
        }

        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: #fff;
        }

        .btn-info {
            background: linear-gradient(135deg, var(--info), var(--info-dark));
            color: #fff;
        }

        .btn-success {
            background: linear-gradient(135deg, var(--success), var(--success-dark));
            color: #fff;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 7px 20px var(--shadow-medium);
        }

        .btn:active {
            transform: translateY(-1px);
        }

        .coming-soon {
            display: inline-block;
            background: #f0f4ff;
            color: var(--primary);
            padding: 8px 18px;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 600;
            margin-top: 15px;
            border: 1px solid #d9e1ff;
        }

        .coming-soon-badge {
            position: absolute;
            top: -12px;
            right: -12px;
            background: #ff6b6b;
            color: #fff;
            padding: 8px 22px;
            border-radius: 25px;
            font-size: 0.85rem;
            font-weight: 700;
            box-shadow: 0 4px 15px var(--shadow-dark);
            z-index: 3;
            animation: pulse 2s infinite;
        }

        @@keyframes pulse {
            0%

        {
            box-shadow: 0 4px 15px rgba(255, 107, 107, 0.6);
        }

        50% {
            box-shadow: 0 4px 20px rgba(255, 107, 107, 0.9);
        }

        100% {
            box-shadow: 0 4px 15px rgba(255, 107, 107, 0.6);
        }

        }

        .disabled-overlay {
            position: relative;
        }

            .disabled-overlay::after {
                content: "";
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(255, 255, 255, 0.85);
                border-radius: 18px;
                z-index: 1;
            }

        .card-content {
            position: relative;
            z-index: 2;
        }

        .modal-content {
            border-radius: 18px;
            box-shadow: 0 15px 40px var(--shadow-dark);
            border: none;
            overflow: hidden;
        }

        .modal-header, .modal-footer {
            border: none;
            padding: 25px 30px;
        }

        .modal-header {
            background: linear-gradient(135deg, #f8f9ff, #f0f2ff);
        }

        .modal-body {
            padding: 40px 30px;
            text-align: center;
        }

            .modal-body h4 {
                font-weight: 600;
                margin-top: 20px;
                color: var(--text-dark);
                font-size: 1.5rem;
            }

            .modal-body .fa-check-circle {
                font-size: 4rem;
                color: var(--success);
                filter: drop-shadow(0 4px 8px rgba(0, 201, 167, 0.3));
            }

        .footer {
            text-align: center;
            margin-top: 50px;
            padding: 20px 0;
            color: var(--text-medium);
            font-size: 0.9rem;
        }

        @@media (max-width: 576px) {
            .app-card

        {
            padding: 25px 20px;
            margin-bottom: 25px;
        }

        .header h1 {
            font-size: 2.2rem;
        }

        .header p {
            font-size: 1.1rem;
        }

        .btn {
            width: 100%;
            margin: 5px 0;
        }

        .card-icon {
            width: 70px;
            height: 70px;
            line-height: 70px;
            font-size: 2rem;
        }

        }
    </style>
</head>
<body>
    <div class="container">
        <!-- HEADER START -->
        <div class="header">
            <h1>Welcome to Municipal Service App</h1>
            <p>Your platform for community services and issue reporting</p>
        </div>
        <!-- HEADER END -->

        <!-- MAIN SECTION XTART -->
        <div class="app-card">
            <div class="card-content">
                <div class="card-icon">
                    <i class="fas fa-exclamation-circle"></i>
                </div>
                <h3>Report Issues</h3>
                <p>Notify us about problems in your neighborhood that need attention</p>
                <a asp-controller="Issue" asp-action="ReportIssue" class="btn btn-primary">
                    Report an Issue
                </a>
            </div>
        </div>

        <div class="app-card">
            <div class="card-content">
                <div class="card-icon info">
                    <i class="fas fa-calendar-alt"></i>
                </div>
                <h3>Local Events & Announcements</h3>
                <p>Stay informed about local events, meetings, and important community activities.</p>
                <a asp-controller="Events" asp-action="Index" class="btn btn-info">
                    View Events
                </a>
            </div>
        </div>

        <div class="app-card disabled-overlay">
            <span class="coming-soon-badge">Coming Soon</span>
            <div class="card-content">
                <div class="card-icon success">
                    <i class="fas fa-clipboard-list"></i>
                </div>
                <h3>Service Request Status</h3>
                <p>Track the progress of your service requests and reported issues</p>
                <a href="#" class="btn btn-success disabled">
                    Check Status
                </a>
                <span class="coming-soon">Feature in development</span>
            </div>
        </div>

        <div class="footer">
            <p>Municipal Service App &copy; 2023 | Serving our community</p>
        </div>
    </div>
    <!-- MAIN SECTION END -->

    <!-- THANK YOU MESSAGE START  -->
    <div class="modal fade" id="thankYouModal" tabindex="-1" aria-labelledby="thankYouModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="thankYouModalLabel">Thank You!</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <i class="fas fa-check-circle mb-3"></i>
                    <h4>Thank you for helping the community!</h4>
                    <p class="mt-3">Your contribution makes our neighborhood a better place for everyone.</p>
                </div>
                <div class="modal-footer justify-content-center">
                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal">OK</button>
                </div>
            </div>
        </div>
    </div>
    <!-- THANK YOU MESSAGE START  -->

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        @if (TempData["ShowThankYou"] != null && (bool)TempData["ShowThankYou"])
        {
                <text>
                    document.addEventListener('DOMContentLoaded', function() {
                        var myModal = new bootstrap.Modal(document.getElementById('thankYouModal'));
                        myModal.show();
                    });
                </text>
        }
    </script>
</body>
</html>